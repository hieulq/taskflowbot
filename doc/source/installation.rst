============
Installation
============

At the command line::

    $ pip install taskflowbot

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv taskflowbot
    $ pip install taskflowbot
